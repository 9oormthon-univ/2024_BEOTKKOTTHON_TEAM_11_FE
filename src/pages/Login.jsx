import styled from 'styled-components';

const Container = styled.div``;

const Login = ({}) => {
    return (
        <Container>
            Login
            <br />
            <a href="/">Home</a>
        </Container>
    );
};

export default Login;
