import styled from 'styled-components';

const Container = styled.div``;

const Register = ({}) => {
    return (
        <Container>
            Register
            <br />
            <a href="/">Home</a>
        </Container>
    );
};

export default Register;
